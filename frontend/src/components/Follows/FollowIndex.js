import { Modal } from "../../context/modal";


const FollowIndex = ({ displayUsernames, count, onClose }) => {

  return (
    <Modal onClose={onClose} >

    </Modal>
  )
};

export default FollowIndex;